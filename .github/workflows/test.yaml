on:
  pull_request:
    branches: [main]

jobs:
  trigger-workflow:
    uses: brighteast99/workflows/.github/workflows/notify.yaml@feat/notify
    with:
      target: discord
      identity: github
      message: test
    secrets:
      NOTIFICATION_WEBHOOK_URL: ${{ secrets.NOTIFICATION_WEBHOOK_URL }}
      WEBHOOK_BASIC_AUTH: ${{ secrets.WEBHOOK_BASIC_AUTH }}
